#include <stdio.h>

int main() {
    // Declaring and initializing a string
    char str1[] = "Hello, World!"; // Implicitly includes the null character
    printf("String: %s\n", str1);  // Output: Hello, World!
    
    return 0;
}
