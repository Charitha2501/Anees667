#include <stdio.h>

int main() {
    // Loop through numbers 1 to 10
    for (int i = 1; i <= 10; i++) {
        printf("%d\n", i);  // Print each number followed by a new line
    }

    return 0;
}
